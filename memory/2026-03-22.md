# 2026-03-22 작업 기록

## 완료
- Y축 소수점 수정: `%` 단위만 `toFixed(2)`, 나머지는 정수 (`Math.round`). WTI는 소수점 제거 요청으로 `v < 100` 조건 삭제. 커밋 `4b7e3c4`
- 개별종목 차트 Y축도 동일 로직 적용 (line 2106). 커밋 `a162690`
- 헤더 레이아웃 정리: 2-row 구조 (`header-row-top` + `header-row-bottom`), `header-right` → `header-actions`, absolute 포지션 제거, 모바일에서 `.btn-label` 숨김 (아이콘만). 커밋 `e56f72d`
- USD/KRW 환율 NAVER 보정: `m.stock.naver.com/front-api/marketIndex/productDetail?category=exchange&reutersCode=FX_USDKRW`에서 `closePrice` 가져와 Yahoo current 대체. `const` → `let` 버그도 수정. 커밋 `0bf45c1`, `aba9437`
- 환율 출처 라벨도 NAVER Finance로 변경 (`isNaver` 조건에 `KRW=X` 추가)

## 사용자 요청 (미착수)
- **대한항공 ICN→JFK 마일리지 프레스티지 좌석 알림 앱**: 공식 API 없음. 선택지를 안내함:
  1. seats.aero API (유료 $10/월) — 가장 현실적
  2. ExpertFlyer (유료, 자체 알림 기능 있음)
  3. 대한항공 직접 스크래핑 (로그인 필요, 캡챠, 불안정)
  - 사용자 결정 대기 중

## 기술 메모
- NAVER 환율 API: `FX_USDKRW` reutersCode, `closePrice`는 쉼표 포함 문자열 (파싱 시 `replace(/,/g, '')` 필요)
- 환율 차트 히스토리는 여전히 Yahoo (NAVER 히스토리 API 접근 불가)
- Y축 콜백 최종 로직: `ind.unit === '%' ? v.toFixed(2) : Math.round(v).toLocaleString('ko-KR')` (주요정보 + 개별종목 동일)
- 최신 커밋: `aba9437` on main
